# online WebView 地址，会编译进应用；不得保存 App Key、签名密码或令牌。
VUE_APP_WEBVIEW_URL=https://example.invalid/

# 独立的完整更新检查接口 URL，不得省略接口路径，也不得回退到 WebView 地址。
VUE_APP_UPGRADE_CHECK_URL=https://upgrade.example.com/api/v1/upgrade/check